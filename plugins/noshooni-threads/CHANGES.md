# CHANGES

## 2026-09-13 · 플러그인으로 묶음 (스폰지클럽 3기 스킬러스)

- `threads-core` · `threads-material` · `threads-writing` 세 스킬을 플러그인 `noshooni-threads` 로 묶었다. 스킬 본문은 그대로 두었다.
- `threads-writing/references/cardnews.md` — claude.ai 전용 저장 경로(`/mnt/user-data/outputs/`)와 도구(`present_files`)를 가리키던 한 문장을 어느 환경에서나 통하는 표현으로 바꿨다. Claude Code 에는 그 경로와 도구가 없어서, 그대로 두면 카드뉴스 저장 단계가 헛돈다.
